const functionName = function(x, y, z) {
  return {

  }
}

const functionName = (x, y, z) => {
  return {

  }
}

const functionName = ({ x, y = 'no', z: label }) => {
  return (
    <div>{x}</div>
  )
}

const functionName = ({ x, y = 'no', z: label }) => <div>{x}</div>;

const functionName = ([x, y = 'no']) => ({ x, y });

const functionName = x => ([x]);

const functionName = ([x, y = 'no']) => {
  return {

  }
}
